/.bundle/
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
/spec/reports/
/tmp/

# rspec failure tracking
.rspec_status

# worktrees
.worktrees/

# test fixture lockfiles (avoid Dependabot noise from ancient transitive deps)
spec/**/Gemfile.lock
# but track lockfile fixtures used by SARIF/diff specs — synthetic content,
# no real gem deps, so Dependabot ignores them
!spec/fixtures/sarif/Gemfile.lock

# claude
.claude/tasks/
/docs/plans
scratch/

# floor-test lockfile (resolved fresh, never committed)
Gemfile.floors.lock

# default output paths when a user runs still_active in-repo (--sarif/--cyclonedx)
/still_active.sarif.json
