#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

# Wire git hooks shared via .githooks/. Pre-push runs bin/check (rubocop + rspec)
# so cross-file lint issues don't escape to CI.
git config core.hooksPath .githooks
